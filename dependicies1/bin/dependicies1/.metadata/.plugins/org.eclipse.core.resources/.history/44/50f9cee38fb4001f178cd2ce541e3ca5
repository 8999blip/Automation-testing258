package testngmaven;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.Test;
import org.testng.reporters.jq.Main;

public class WebTableTestCases {
	
	@Test
	public void testInvalidCreadentialsLoginpage() {
		Object alExpResult;
		((Object) alExpResult).add("Category ID");
		((Object) alExpResult).add("Category Name");
		((Object) alExpResult).add("Creatd at");
		((Object) alExpResult).add("Modified At");
		((Object) alExpResult).add("Actions");
		
		WebDriver driver = new ChromeDriver();
		driver.get("https://javabykiran.com/selenium/admin.html#logout");
		for (int i = 1; i <=4; i++) {
			WebElement actColumn = driver.findElement(By.xpath("//*[@id=\"categories-table\"]/thead/tr/th[1]"));
		
		}
		
	}


}
